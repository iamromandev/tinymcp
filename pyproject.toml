[project]
name = "tinymcp"
version = "0.1.0"
description = "A simple MCP server with streamable HTTP transport and Auth0 OAuth"
requires-python = ">=3.11"
dependencies = [
    "fastmcp",
    "python-dotenv",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
